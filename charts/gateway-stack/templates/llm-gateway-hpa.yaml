{{- if and .Values.enabled .Values.llmGateway.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "gateway-stack.fullname" . }}-llm-gateway
  labels:
    {{- include "gateway-stack.labels" . | nindent 4 }}
    app.kubernetes.io/component: llm-gateway
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "gateway-stack.fullname" . }}-llm-gateway
  minReplicas: {{ .Values.llmGateway.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.llmGateway.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.llmGateway.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.llmGateway.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.llmGateway.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.llmGateway.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
